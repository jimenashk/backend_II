server:
  port: 8761
spring:
  application:
    name: eureka-server
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
eureka:
  client:
    fetchRegistry: false
    registerWithEureka: false